var rain = [];

var updatendisplay = function(Raindrop) {
  Raindrop.update();
  Raindrop.display();
};

setup = function () {
  createCanvas(600, 600);
  // your code goes here
};

draw = function () {
  background(0);
    var index = 0;
    while (mou)
  // your code goes here
};
